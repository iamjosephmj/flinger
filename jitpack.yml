jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.10-tem
  - sdk use java 17.0.10-tem
install:
  # Build and publish only the library module so an :app build failure
  # can't break the release, and JitPack ships a single artifact.
  - ./gradlew :flinger:publishToMavenLocal
